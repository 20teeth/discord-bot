# Required Commands and Tasks

- [x] Implement `/ban` to ban up to 5 users in one command (with confirmation and a reason), and `/unban`.
- [x] Implement `/mute` to mute up to 5 users or all server users with at least 3 moderator approvals, and `/unmute`.
- [x] Implement `/post` to publish an approved project from Projects and sync it to the website.
- [x] Make `/purge` delete messages.
- [x] Document the bot.

Notes:
- No log channels are used; notifications are sent via user DM and ephemeral replies.